{
  "name": "@chronoscope/replay-engine",
  "version": "1.0.0",
  "description": "Replay Engine — The core innovation. Fetches ordered events from the Event Store and re-executes them deterministically with idempotency and side-effect isolation. Supports dry-run and state-rebuild modes.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.12.0",
    "ioredis": "^5.3.2",
    "@chronoscope/core": "file:../core"
  }
}
