{
  "name": "@chronoscope/payment-service",
  "version": "1.0.0",
  "description": "Payment Service — Consumes ORDER_CREATED events from Kafka, processes payments, and emits payment lifecycle events. Demonstrates side-effect isolation during replay.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@chronoscope/core": "file:../../core"
  }
}
